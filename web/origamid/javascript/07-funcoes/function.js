//s funções (Function) são blocos de código que podem ser executados. A execução pode acontecer em diferentes cenários e diversas vezes. Para executar utilizamos os parênteses ();
function logCurso() {
  const nome = 'JavaScript';
  const horas = '40 horas';
  console.log(nome, horas);
}

logCurso();

